# Archive — historical documents, not authoritative

These files were carried over from the original `XmlSpawner-for-Modernuo` repository
(branch `kb/modern_spawner`, commit `2b5193b`, April 2026) when the project moved here.

They describe design intent and status **as of that time**. The code has moved on and the
status claims were never re-verified. Treat every statement here as a hypothesis to check
against `Projects/ModernSpawner/`, not as a spec. The current specs live one level up in
`dev-docs/`.
